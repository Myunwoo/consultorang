import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const MenuCalculatorScreen = (({navigation}) => {
    return (
        <View>
            <Text>메뉴 계산기 페이지</Text>
        </View>
    );
});

export default MenuCalculatorScreen;