import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const IncomeStatementScreen = (({navigation}) => {
    return (
        <View>
            <Text>손익 계산서 페이지</Text>
        </View>
    );
});

export default IncomeStatementScreen;