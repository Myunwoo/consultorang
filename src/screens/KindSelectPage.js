import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const KindSelectPage = (({navigation}) => {
    return (
        <View>
            
        </View>
    );
});

export default KindSelectPage;