import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const EmailFindScreen = (({navigation}) => {
    return (
        <View>
            <Text>아이디 찾기</Text>
        </View>
    );
});

export default EmailFindScreen;