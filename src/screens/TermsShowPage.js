import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const TermsShowPage = (({navigation}) => {
    return (
        <View>
            
        </View>
    );
});

export default TermsShowPage;